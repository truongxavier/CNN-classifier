#on se met dans le répertoire mlflow
cd streamlit

#on créé le env dans le répertoire mlflow
python3 -m venv pres_env  

source pres_env/bin/activate

pip install -r requirements.txt

streamlit run presentation.py

docker logs -f loving_panini

#donner les droit a son utilisateur pour la copie
sudo chown -R xavier:xavier /home/xavier/code/truongxavier/CNN-classifier/bentoml_service/data